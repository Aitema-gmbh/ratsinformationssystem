# aitema|Rats RIS – Umgebungsvariablen
# Kopiere diese Datei zu .env und passe die Werte an:
#   cp .env.example .env

# ================================
# Datenbank (PFLICHT)
# ================================
DB_PASSWORD=bitte_aendern_sicheres_passwort
DATABASE_URL=postgresql://aitema:${DB_PASSWORD}@db:5432/ris

# ================================
# Anwendung (PFLICHT)
# ================================
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NODE_ENV=production
PORT=3000

# ================================
# OParl-Schnittstelle
# ================================
# URL einer externen OParl-Quelle (optional)
OPARL_API_URL=
# API-Schlüssel falls erforderlich
OPARL_API_KEY=

# ================================
# Cache
# ================================
# Cache-Lebensdauer in Sekunden (Standard: 300 = 5 Minuten)
CACHE_TTL_SECONDS=300

# ================================
# E-Mail (optional)
# ================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=ris@ihre-gemeinde.de

# ================================
# Suche (optional)
# ================================
# Volltext-Suche aktivieren (erfordert mehr RAM)
ENABLE_FULLTEXT_SEARCH=true
