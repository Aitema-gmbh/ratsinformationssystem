# ==========================================
# aitema|Rats – Produktions-Konfiguration
# ==========================================
#
# WICHTIG: Diese Datei ist ein Template!
# Kopieren: cp .env.production.example .env.production
# NIEMALS .env.production in Git einchecken!
#
# OParl 1.1 konformes Ratsinformationssystem
#
# Sichere Passwörter generieren:
#   openssl rand -hex 32      (für Passwörter/Secrets)
#   openssl rand -base64 32   (für Base64-Secrets)
#   openssl rand -hex 64      (für lange Secrets)

# ================================
# PostgreSQL (PFLICHT)
# ================================
POSTGRES_USER=ris_user
POSTGRES_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32
POSTGRES_DB=ris_db

# ================================
# Redis (PFLICHT)
# ================================
REDIS_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32

# ================================
# Elasticsearch (PFLICHT)
# ================================
ES_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32

# ================================
# MinIO Objektspeicher (PFLICHT, für Dokumente/PDFs)
# ================================
MINIO_ACCESS_KEY=ris_minio_user
MINIO_SECRET_KEY=BITTE_AENDERN_openssl_rand_hex_32
MINIO_BUCKET=ris-documents

# ================================
# Keycloak SSO (PFLICHT)
# ================================
KEYCLOAK_ADMIN=kcadmin
KEYCLOAK_ADMIN_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32
KEYCLOAK_HOSTNAME=sso.IHRE-DOMAIN.de
KEYCLOAK_REALM=ris
KEYCLOAK_CLIENT_ID=ris-frontend

# ================================
# Anwendungs-Secrets (PFLICHT)
# ================================
SECRET_KEY=BITTE_AENDERN_openssl_rand_hex_64

# ================================
# OParl-Konfiguration (PFLICHT)
# ================================
# ID Ihrer Organisation (wird in OParl-Objekte eingebettet)
OPARL_BODY_ID=https://ris.IHRE-DOMAIN.de/api/oparl/v1.1/body/1
OPARL_SYSTEM_URL=https://ris.IHRE-DOMAIN.de/api/oparl/v1.1/system

# ================================
# CORS und URLs (PFLICHT)
# ================================
CORS_ORIGINS=https://ris.IHRE-DOMAIN.de
PUBLIC_API_URL=https://ris.IHRE-DOMAIN.de/api
PUBLIC_KEYCLOAK_URL=https://sso.IHRE-DOMAIN.de

# ================================
# E-Mail (für Benachrichtigungen)
# ================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=ris@IHRE-DOMAIN.de
SMTP_PASSWORD=SMTP_PASSWORT_HIER_EINTRAGEN
SMTP_FROM=aitema|Rats <ris@IHRE-DOMAIN.de>

# ================================
# Sicherheit
# ================================
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=1m

# ================================
# Backup (optional)
# ================================
BACKUP_RETENTION_DAYS=30

# ================================
# Monitoring (optional)
# ================================
# Sentry DSN für Error-Tracking
# SENTRY_DSN=https://...@sentry.io/...

# ================================
# Logging
# ================================
LOG_LEVEL=info
